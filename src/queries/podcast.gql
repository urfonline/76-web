query QPodcast($slug: String!) {
    podcast(slug: $slug) {
        title, description, coverUrl, spotifyUrl
        episodes {
            title, description, createdAt, duration, mediaUrl
        }
    }
}
